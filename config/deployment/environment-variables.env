# =============================================================================
# ENVIRONMENT VARIABLES CONFIGURATION
# =============================================================================
# This file serves as documentation and template for environment variables
# NEVER commit actual secret values to version control
# Use your deployment platform's environment variable configuration instead

# =============================================================================
# CORE APPLICATION SETTINGS
# =============================================================================
NODE_ENV=production
HUGO_ENV=production
BUILD_MODE=production
DEBUG=false

# Site Configuration
SITE_NAME=Portfolio Site
SITE_URL=https://your-domain.com
API_BASE_URL=https://your-domain.com/api
CORS_ORIGIN=https://your-domain.com

# Build Configuration
HUGO_VERSION=0.121.0
NODE_VERSION=18
HUGO_CACHEDIR=/tmp/hugo_cache
COMPRESSION_ENABLED=true

# =============================================================================
# DATABASE & BACKEND SERVICES
# =============================================================================
# Supabase Configuration (Public keys are safe for client-side)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=eyJ...your-anon-key  # Public key, safe for client-side

# SECRET KEYS (Never commit these values)
SUPABASE_SERVICE_ROLE_KEY=eyJ...your-service-role-key  # Server-side only
DATABASE_URL=postgresql://...  # If using external database
REDIS_URL=redis://...  # If using Redis for caching

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================
# Auth0 Configuration (if using)
AUTH0_DOMAIN=your-domain.auth0.com
AUTH0_CLIENT_ID=your-client-id
AUTH0_CLIENT_SECRET=your-client-secret  # SECRET
AUTH0_BASE_URL=https://your-domain.com
AUTH0_SECRET=your-auth0-secret  # SECRET

# JWT Configuration
JWT_SECRET=your-jwt-secret  # SECRET - Generate a strong random key
JWT_EXPIRES_IN=7d
REFRESH_TOKEN_EXPIRES_IN=30d

# Session Configuration
SESSION_SECRET=your-session-secret  # SECRET - Generate a strong random key
COOKIE_DOMAIN=your-domain.com
COOKIE_SECURE=true
COOKIE_SAME_SITE=strict

# =============================================================================
# EXTERNAL SERVICES & APIs
# =============================================================================
# Email Service (SendGrid, Mailgun, etc.)
SENDGRID_API_KEY=your-sendgrid-key  # SECRET
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-email-password  # SECRET

# Cloud Storage (if using)
AWS_ACCESS_KEY_ID=your-aws-key  # SECRET
AWS_SECRET_ACCESS_KEY=your-aws-secret  # SECRET
AWS_REGION=us-east-1
AWS_S3_BUCKET=your-bucket-name

# CDN Configuration
CDN_URL=https://your-cdn.com
IMAGE_CDN_URL=https://images.your-cdn.com

# =============================================================================
# MONITORING & ANALYTICS
# =============================================================================
# Error Tracking (Sentry)
SENTRY_DSN=https://your-sentry-dsn  # Can be public
SENTRY_ENVIRONMENT=production
SENTRY_RELEASE=1.0.0

# Analytics
GOOGLE_ANALYTICS_ID=GA-XXXXXXXXX-X  # Public
GOOGLE_TAG_MANAGER_ID=GTM-XXXXXXX  # Public
HOTJAR_ID=123456  # Public

# Performance Monitoring
NEW_RELIC_LICENSE_KEY=your-newrelic-key  # SECRET
DATADOG_API_KEY=your-datadog-key  # SECRET

# =============================================================================
# DEPLOYMENT & CI/CD
# =============================================================================
# Netlify Configuration
NETLIFY_AUTH_TOKEN=your-netlify-token  # SECRET
NETLIFY_SITE_ID=your-site-id

# Vercel Configuration
VERCEL_TOKEN=your-vercel-token  # SECRET
VERCEL_ORG_ID=your-org-id
VERCEL_PROJECT_ID=your-project-id

# GitHub Actions
GITHUB_TOKEN=your-github-token  # SECRET
CODECOV_TOKEN=your-codecov-token  # SECRET

# =============================================================================
# FEATURE FLAGS & CONFIGURATION
# =============================================================================
ENABLE_ANALYTICS=true
ENABLE_CONTACT_FORM=true
ENABLE_SEARCH=true
ENABLE_BLOG=true
ENABLE_PORTFOLIO=true
ENABLE_ADMIN_PANEL=true
MAINTENANCE_MODE=false

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX_REQUESTS=100

# Caching
CACHE_TTL=3600  # 1 hour
REDIS_CACHE_ENABLED=false
MEMORY_CACHE_ENABLED=true

# =============================================================================
# DEVELOPMENT & DEBUGGING
# =============================================================================
# Only for development environments
LOG_LEVEL=info
LOG_FORMAT=json
PRETTY_LOGS=false
ENABLE_REQUEST_LOGGING=true
ENABLE_ERROR_STACK_TRACE=false

# Testing Configuration
TEST_DATABASE_URL=postgresql://...  # For test environment only
MOCK_EXTERNAL_APIS=false

# =============================================================================
# PLATFORM-SPECIFIC CONFIGURATIONS
# =============================================================================

# Netlify Functions
NETLIFY_FUNCTIONS_ENABLED=true
NETLIFY_IDENTITY_ENABLED=false

# Vercel Functions
VERCEL_FUNCTIONS_ENABLED=true
VERCEL_REGIONS=iad1,sfo1

# Edge Functions
EDGE_FUNCTIONS_ENABLED=true

# =============================================================================
# BACKUP & RECOVERY
# =============================================================================
BACKUP_ENABLED=true
BACKUP_SCHEDULE=daily
BACKUP_RETENTION_DAYS=30
BACKUP_STORAGE_TYPE=s3  # s3, gcs, azure

# =============================================================================
# SECURITY HEADERS & POLICIES
# =============================================================================
ENABLE_SECURITY_HEADERS=true
ENABLE_CSP=true
ENABLE_HSTS=true
ENABLE_CORS=true

# Content Security Policy
CSP_SCRIPT_SRC=self unsafe-inline unsafe-eval https:
CSP_STYLE_SRC=self unsafe-inline https:
CSP_IMG_SRC=self data: https:
CSP_CONNECT_SRC=self https:

# =============================================================================
# PERFORMANCE OPTIMIZATION
# =============================================================================
ENABLE_COMPRESSION=true
ENABLE_MINIFICATION=true
ENABLE_IMAGE_OPTIMIZATION=true
ENABLE_LAZY_LOADING=true

# Service Worker
ENABLE_SERVICE_WORKER=true
CACHE_STRATEGY=cacheFirst

# =============================================================================
# LOCALIZATION
# =============================================================================
DEFAULT_LANGUAGE=en
SUPPORTED_LANGUAGES=en,es
ENABLE_RTL=false
TIMEZONE=UTC

# =============================================================================
# ADMIN PANEL CONFIGURATION
# =============================================================================
ADMIN_PANEL_PATH=/admin
ADMIN_CACHE_BUSTING=true
ADMIN_EMERGENCY_ACCESS=true
ADMIN_SESSION_TIMEOUT=3600  # 1 hour

# =============================================================================
# CONTACT FORM CONFIGURATION
# =============================================================================
CONTACT_FORM_RECIPIENT=admin@your-domain.com
CONTACT_FORM_SUBJECT_PREFIX=[Portfolio Contact]
ENABLE_SPAM_PROTECTION=true
RECAPTCHA_SITE_KEY=your-recaptcha-site-key  # Public
RECAPTCHA_SECRET_KEY=your-recaptcha-secret  # SECRET

# =============================================================================
# SEARCH CONFIGURATION
# =============================================================================
SEARCH_PROVIDER=local  # local, algolia, elasticsearch
ALGOLIA_APP_ID=your-algolia-app-id  # If using Algolia
ALGOLIA_API_KEY=your-algolia-key  # SECRET
ALGOLIA_INDEX_NAME=portfolio_search

# =============================================================================
# WEBHOOKS & INTEGRATIONS
# =============================================================================
WEBHOOK_SECRET=your-webhook-secret  # SECRET
SLACK_WEBHOOK_URL=https://hooks.slack.com/...  # SECRET
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...  # SECRET

# =============================================================================
# USAGE NOTES
# =============================================================================
# 1. Copy this file to create environment-specific configurations:
#    - .env.local (for local development)
#    - .env.staging (for staging environment)
#    - .env.production (for production environment)
#
# 2. Replace all placeholder values with actual values
#
# 3. Add all SECRET values to your deployment platform's environment variables:
#    - Netlify: Site settings > Environment variables
#    - Vercel: Project settings > Environment Variables
#    - GitHub Actions: Repository > Settings > Secrets and variables > Actions
#
# 4. Never commit files containing actual secret values to version control
#
# 5. Regularly rotate secret keys and tokens
#
# 6. Use different values for different environments (dev, staging, production)
#
# 7. Validate environment variables on application startup